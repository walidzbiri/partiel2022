<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zbiri.walid.partiel2022" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ApplicationContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Partiel2022" >
        <activity android:name=".ToolBar" >
        </activity>
        <activity android:name=".DriverDetails" android:theme="@style/Theme.Partiel2022.NoActionBar"/>
        <activity android:name=".DriverItem" />
        <activity
            android:theme="@style/Theme.Partiel2022.NoActionBar"
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>